#include <stdio.h>
/**
*main - starting point
*Return: zero is good
*/
int main(void)
{
	char alpha;
	for(alpha = 'a' ; alpha <= 'z' ; alpha++) {

		putchar(alpha);
	}
	putchar ('\n');
	return (0);
}
