#include "main.h"
/**
* add - adds two numbers
* @a: first argument
* @b: second argument
* Description: adds two integers and returns the result
* Return: value of function
*/
int add(int a, int b)
{
	return (a + b);
}
